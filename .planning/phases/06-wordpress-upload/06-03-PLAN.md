---
phase: 06-wordpress-upload
type: execute
---

<objective>
Update the /create-location-page Claude command to include WordPress upload confirmation flow.

Purpose: This completes the CLI experience — generate content, review, confirm upload, done.
Output: Updated `.claude/commands/create-location-page.md` with upload step
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/06-wordpress-upload/06-01-SUMMARY.md
@.planning/phases/06-wordpress-upload/06-02-SUMMARY.md
@.claude/commands/create-location-page.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /create-location-page command with upload flow</name>
  <files>.claude/commands/create-location-page.md</files>
  <action>
    Update the existing Claude command to add WordPress upload as a follow-up action after content generation:

    1. Keep the existing content generation flow (steps 1-3) exactly as-is
    2. Replace the "coming soon" WordPress option in the follow-up actions with a working flow:
       a. After showing generation results, ask: "Would you like to upload this to WordPress as a draft?"
       b. If user says yes:
          - Call `POST http://localhost:3000/api/v1/wp-upload` with the same keyword and pageType
          - Report: WordPress URL, draft status, content type (page/post)
          - Remind user to review the draft in WordPress before publishing
       c. If user says no: end normally (same as current behavior)

    3. Keep the "read the generated file" option as well

    IMPORTANT: The command must handle the case where WordPress env vars aren't configured — if the upload returns a 500/502, show a helpful message about setting up WP_BASE_URL, WP_USERNAME, WP_APP_PASSWORD in .local.env.
  </action>
  <verify>Read the updated command file; confirm it has the upload flow with curl to /api/v1/wp-upload</verify>
  <done>Command asks for upload confirmation after generation, calls wp-upload endpoint on confirm, handles errors gracefully</done>
</task>

<task type="auto">
  <name>Task 2: Update README with WordPress upload documentation</name>
  <files>README.md</files>
  <action>
    Update the README to reflect the completed WordPress upload feature:

    1. In "Current Implementation Status" section, add a new subsection for WordPress Upload (Phase 6) — mark as complete
    2. In "API Documentation", add the POST /api/v1/wp-upload endpoint with request/response examples
    3. In "Environment Variables", add WP_BASE_URL, WP_USERNAME, WP_APP_PASSWORD
    4. Update the "Next Steps / Roadmap" section — mark Milestone 2 as complete
    5. Update the "Last Updated" line at the bottom

    Keep documentation concise — follow the existing style (PowerShell examples, JSON response blocks).
  </action>
  <verify>README contains /wp-upload endpoint docs, WP env vars documented, Milestone 2 marked complete</verify>
  <done>README fully documents the WordPress upload feature, endpoint, env vars, and updated roadmap</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete WordPress upload flow via /create-location-page command</what-built>
  <how-to-verify>
    1. Ensure dev server is running: `npm run dev`
    2. Ensure .local.env has valid WordPress credentials
    3. Run the Claude command: `/create-location-page [keyword] [city] [state]`
    4. Wait for content generation to complete
    5. When prompted "Upload to WordPress?", confirm yes
    6. Check WordPress admin — verify draft exists with correct HTML content
    7. Verify the content type is correct (page for service_page, post for blog)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 6 complete, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `/create-location-page` command includes upload confirmation flow
- [ ] Upload error handling works (missing WP credentials shows helpful message)
- [ ] README documents the new endpoint, env vars, and updated roadmap
- [ ] Full flow works: generate → confirm → upload → draft appears in WordPress
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Claude command provides smooth UX: generate → ask → upload → report
- README is accurate and up to date
- No regressions to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-wordpress-upload/06-03-SUMMARY.md`
</output>

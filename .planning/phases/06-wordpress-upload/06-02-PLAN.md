---
phase: 06-wordpress-upload
type: execute
---

<objective>
Wire up the wp-upload API endpoint: controller, route, and Express mounting.

Purpose: Expose the WordPress upload service as an HTTP endpoint the Claude command can call.
Output: `POST /api/v1/wp-upload` endpoint fully functional
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/06-wordpress-upload/06-01-SUMMARY.md
@src/controllers/text_media_creator.controller.ts
@src/routes/text_media_creator.routes.ts
@src/app.ts
@src/controllers/index.ts
@src/routes/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WordPress upload controller</name>
  <files>src/controllers/wordpress_upload.controller.ts, src/controllers/index.ts</files>
  <action>
    Create controller following the project's object-literal pattern (see text_media_creator.controller.ts as reference):

    `wordpressUploadController` object with `uploadContent` method:
    1. Extract `keyword` and `pageType` from request body (pageType defaults to "service_page")
    2. Validate keyword exists, return 400 if missing
    3. Call `WordPressUploadService.uploadToWordPress({ keyword, pageType, status: "draft" })`
    4. Return success response matching project's standard format:
       ```
       { success: true, data: { ...WordPressUploadResponse }, message: "Content uploaded to WordPress as draft" }
       ```
    5. Catch errors: return 404 if markdown file not found, 502 if WordPress API fails, 500 for unexpected errors

    Export from controllers/index.ts barrel file.
  </action>
  <verify>npx tsc --noEmit passes; controller exports uploadContent method</verify>
  <done>Controller validates input, calls service, returns formatted response with proper error codes</done>
</task>

<task type="auto">
  <name>Task 2: Create route and mount in Express app</name>
  <files>src/routes/wordpress_upload.routes.ts, src/routes/index.ts, src/app.ts</files>
  <action>
    1. Create route file following existing pattern (see text_media_creator.routes.ts):
       - POST `/v1/wp-upload` → `wordpressUploadController.uploadContent`

    2. Export from routes/index.ts barrel file

    3. Mount in app.ts alongside existing routes:
       - `app.use('/api', wordpressUploadRoutes)`

    Follow the EXACT same patterns as existing route files — same Router import, same export style.
  </action>
  <verify>npx tsc --noEmit passes; grep for '/wp-upload' in route file confirms endpoint exists</verify>
  <done>POST /api/v1/wp-upload route exists, mounted in Express app, compiles clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>WordPress upload endpoint wired up and ready to test</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Test with a keyword that has a generated article in `src/repositories/final/`:
       ```
       curl -X POST http://localhost:3000/api/v1/wp-upload -H "Content-Type: application/json" -d '{"keyword":"your-existing-keyword", "pageType":"service_page"}'
       ```
    3. Check WordPress admin — should see a new draft page with the article content
    4. Verify HTML formatting looks correct in WordPress editor
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 06-03, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `npm run build` compiles successfully
- [ ] POST /api/v1/wp-upload returns proper response for valid keyword
- [ ] Returns 400 for missing keyword
- [ ] Returns 404 for keyword with no generated article
- [ ] Health endpoint still works (no regression)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Endpoint follows project's existing patterns (response format, error handling)
- No regressions to existing endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/06-wordpress-upload/06-02-SUMMARY.md`
</output>
